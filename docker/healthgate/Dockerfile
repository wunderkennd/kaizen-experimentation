# kaizen-healthgate: init container that gates ECS service startup on
# upstream dependency health. The gate command (generated by
# infra/pkg/aws/compute/services.go buildHealthGateCmd) needs only busybox
# sh, wget --spider, and nc.
FROM busybox:1.37
CMD ["sh"]

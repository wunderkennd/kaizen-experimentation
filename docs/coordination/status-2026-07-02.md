# Repository Status & Next Steps — 2026-07-02

> **Snapshot owner:** repo-status review (branch `claude/repo-status-next-steps-mze2fh`).
> **Scope:** post-Phase-5 state. Phase 5 (ADRs 011–025) is DONE — see `status.md` for that history.
> This document covers the **currently open work**: 37 open issues, 6 open PRs, and the
> Goals roadmap (#647–#655) seeded by `scripts/projects/seed-goals.sh`.
>
> Work is tracked in **GitHub Issues + Projects/Goals**, not in this file. This is a
> point-in-time snapshot to orient the next work session; the issues are the source of truth.

---

## 1. Executive summary

Phase 5 shipped (204 PRs total; 41 in Phase 5). Current work is organized under **nine Goals**
(#647–#655), each mapping to an ADR or initiative. The near-term state:

| Stream | Goal | State | Immediate next step |
| --- | --- | --- | --- |
| ConnectRPC pilot (ADR-031) | #648 | **Active — PRs open** | Merge green PR **#663** (closes #641); close duplicate **#661** |
| Infrastructure GA + GCP expansion | #647 | Active (sprint-I.3) | GCP Phase 3/4 (#496/#498/#500/#501/#502); P2 hygiene #620 |
| ADR-026 Custom Metrics (Phase 3) | #649 | Phases 1–2 shipped; Phase 3 migration open | #601 → #602 (breaking proto change) |
| M5 Rust port (ADR-025) | — (#590, P2) | **Stuck at Phase 2** | Doc fix (1 hr) → RBAC port → Phase 3 decision |
| ADR-027 TOST | #650 | Core landed (#443) | M5/M6 wiring (verify remaining scope) |
| ADR-028/029/030 | #651/#652/#653 | **Proposed (design)** | ADR-029 gated on RFCs #543/#544/#545 |
| QoE Observability GA | #654 | Ingest primitives landed | GA wiring |
| Palette / M6 | #655 | Ongoing | Close duplicate PRs #664/#665/#666 |

**Two duplicate-PR clusters need resolution first** (see §3) — they are the cheapest, highest-signal
cleanup and one of them (ADR-031) unblocks an entire sprint.

---

## 2. Open issues by Goal (37 total)

### Goal #648 — ADR-031 ConnectRPC Pilot *(active, has open PRs)*
Serve M1 assignment over ConnectRPC (Connect/gRPC/gRPC-Web), retire the hand-rolled JSON shim.

| # | Title | Owner | State |
| --- | --- | --- | --- |
| #641 | GetAssignment over Connect (tracer bullet) | agent-1 | **PR #663 green, ready to merge** |
| #642 | Remaining unary RPCs (GetAssignments, GetSlateAssignment, GetInterleavedList) | agent-1 | Blocked on #641 · contract-test |
| #643 | StreamConfigUpdates (server-streaming) | agent-1 | Blocked on #641 · contract-test |
| #644 | Retire JSON shim + server-go client | agent-1 | After #642/#643 · contract-test |
| #645 | Evaluate vs success/kill criteria → fleet decision | agent-1 | **HITL** (rfc) |

**Critical path:** #663 → #641 closed → #642 + #643 → #644 → #645 (human decision).

### Goal #647 — Infrastructure GA (Pulumi/AWS) + GCP expansion *(sprint-I.3)*
AWS GA is done (all 9 ECR repos, services, mock suite green). Open children are a **GCP Phase 3/4** track.

| # | Title | Owner | State |
| --- | --- | --- | --- |
| #496 | GCP edge module (LB, DNS, managed certs, Cloud Armor) | infra-5 | Open |
| #498 | GCP observability (Logging/Monitoring + Managed Prometheus + Grafana) | infra-5 | Open |
| #500 | Smoke load test — M1/M7 p99 < 5ms on Cloud Run | infra-4 | Open |
| #501 | M4b chaos test — kill GCE instance, recover < 10s | infra-4 | Open |
| #502 | M2 throughput — 100K events/sec via Redpanda on GCP | infra-3 | Open |
| #499 | Tenant config wiring — cloudProvider selector + first GCP customer stack | — | **needs-human-input** |
| #503 | Security review + parity audit (AWS vs GCP) | — | **needs-human-input** |

**Infra hygiene (not linked to the Goal):** #620 (CI credential-expiry monitoring, **P2**),
#616 (mirror `postgres:16-alpine` into GAR to drop Docker Hub dep), #513 (Redpanda IPv6 flake on macOS/Colima).

### Goal #649 — ADR-026 Custom Metrics Layer
Phase 1 (Rust M5 + M6 + M3 topo-order) and Phase 2 (#435 MetricQL) shipped. **Phase 3 = deprecating legacy `CUSTOM`.**
> ⚠️ **Housekeeping:** this Goal has *no linked child issues* even though the work below exists — link them so the Goal progress bar tracks.

| # | Title | Owner | State |
| --- | --- | --- | --- |
| #601 | Phase 3 / Phase E operational — CUSTOM survey, proposals, apply migrations | — | P3 |
| #602 | #437.1 remove `MetricType::Custom` from proto (**breaking**) | agent-3/5/6 | P3 · gated on #601 |
| #599 | #437.2 extend translator Tier-1/Tier-2 pattern library | agent-3 | P3 |
| #600 | #437.3 M6 migration-progress dashboard | agent-6 | P4 |
| #554 | Migrate UI MetricDefinition/MetricType to generated proto-es v2 types | agent-6 | P3 |
| #558 | `metric_computation_status` TTL policy (unbounded growth) | — | P3 |
| #476 | Future ADR — migrate flat type-config fields into `oneof type_config` | — | P3 |

### Goals #650–#655 — later ADRs / initiatives
| Goal | ADR / initiative | State | Notes |
| --- | --- | --- | --- |
| #650 | ADR-027 TOST equivalence | Core landed (#443, `tost.rs`) | Verify remaining M5/M6 wiring; no open child issues |
| #651 | ADR-028 M4b shadow inference | **Proposed** | Design phase; no child issues yet |
| #652 | ADR-029 cross-modal calibration | **Proposed** | New `experimentation-calibration` crate; **gated on RFCs below** |
| #653 | ADR-030 shadow experiment mode | **Proposed** | Design phase; no child issues yet |
| #654 | QoE Observability GA | Ingest primitives landed | `ebvs_detected` + `HeartbeatSessionizer` done; GA wiring remains |
| #655 | Palette / M6 design-system standardization | Ongoing | Related PRs #664/#665/#666 (all duplicates — see §3) |

### RFCs — decisions that gate ADR-029 / cluster-g (Personalization)
| # | Title | State |
| --- | --- | --- |
| #543 | RFC-001 Personalization Service Event Emission Boundary | Needs decision |
| #544 | RFC-002 Slate-Bandit Composition Path | Needs decision |
| #545 | RFC-003 Strategy Configuration Contract | Needs decision |

### Cross-cutting P2 tracking
- **#590 (P2)** — ADR-025 M5 Rust port stuck at Phase 2. Documented status drift across 3 sources
  (ADR header says "Proposed", CLAUDE.md says "executed", the binary logs "Phase 2"). Three sub-deliverables:
  1. **Doc/status fix** — update ADR-025 to a per-phase table + fix CLAUDE.md. *~1 hr, no code.*
  2. **RBAC interceptor port** — Rust M5 has **no auth module**; silent security gap if traffic ever shifts to it. *~1–2 days, agent-5.*
  3. **Phase 3 scoping decision** — commit / defer / rollback. *HITL.*

### Tooling / CI backlog
- #521 — `_ready` should exclude issues with active multiclaude workers (not just open PRs).
- #522 — scope test-stack compose deps per suite (don't spin up Redpanda for non-Kafka PRs).

---

## 3. Open PRs — resolve duplicates first (6 open)

| PR | Title | CI | Recommendation |
| --- | --- | --- | --- |
| **#663** | ADR-031 pilot — GetAssignment over Connect, end-to-end | ✅ **all green** (rust/go/ts/infra/hash-parity) | **Merge** → closes #641, unblocks #642–#645 |
| #661 | ADR-031 pilot — GetAssignment (tracer bullet) | ❌ **rust failed** (2026-07-01) | **Close as superseded by #663** (duplicate; both `Closes #641`) |
| #666 | 🎨 Palette: Audit Log accessibility & utility | ✅ (ts/infra) | **Close** — superseded by merged #662 + #636 |
| #665 | 🎨 Palette: Standardize CopyButton + Audit Log a11y | (Jules) | **Close** — duplicate of #662/#636/#664/#666 |
| #664 | 🎨 Palette: Audit Log accessibility & utility | (Jules) | **Close** — duplicate of #662/#636 |
| #632 | Add Claude Code GitHub Workflow | (bot) | Owner decision — merge to enable `@claude`, or close |

> #664/#665/#666 are three near-identical autonomous (Jules) re-runs of the **same** Audit Log
> accessibility task that already merged via **#662** ("add aria-label and empty-state testid") and
> **#636** ("Improve accessibility and visual affordance in Audit Log"). Confirm no unique diff, then close.

---

## 4. Prioritized next steps

### P0 — Immediate (low-risk cleanup + unblock; do this session)
1. **Merge PR #663** (green) → closes #641 → unblocks the whole ConnectRPC chain. **Close PR #661** (red, duplicate).
2. **Close duplicate Palette PRs #664/#665/#666** as superseded by #662/#636 (quick diff-confirm first). Decide #632.
3. **#590 sub-deliverable 1** — ADR-025 + CLAUDE.md status-drift doc fix. ~1 hr, no code, removes a documented trap for new agents.
4. **Link child issues to their Goals** — e.g. attach #599–#602/#554/#558 to Goal #649 so progress bars work.

### P1 — Near-term (active sprints)
5. **ConnectRPC:** after #641 merges, take **#642** (remaining unary RPCs) then **#643** (StreamConfigUpdates). agent-1, contract-test.
6. **Infra GCP Phase 3/4:** #496, #498 (infra-5) → then load/chaos/throughput #500/#501/#502. Plus **#620 (P2)** CI credential monitoring.
7. **#590 sub-deliverable 2 (P2):** port the RBAC interceptor into Rust M5 — closes a silent auth gap. agent-5.
8. **ADR-026 Phase 3:** run #601 (Phase E survey + migrations) → then the breaking #602 (remove `MetricType::Custom`), with #599/#600 supporting.

### P2 — Decisions required (HITL / RFC) — unblock design-phase goals
9. **Accept/reject RFCs #543/#544/#545** — gates ADR-029 (cross-modal calibration / cluster-g).
10. **#590 sub-deliverable 3** — ADR-025 Phase 3 scoping decision (commit / defer / rollback).
11. **#645** — ConnectRPC pilot fleet-wide decision (after #644).
12. **#499 / #503** — GCP tenant selector + AWS/GCP parity & security audit.
13. **Scope ADR-028 / ADR-030** into child issues once designs firm up.

### P3 — Backlog
14. CI/tooling: #521, #522, #513, #616. UI: #554. Retention: #558. Future ADR: #476.

---

## 5. Observations / risks

- **Duplicate autonomous PRs** (2× ADR-031, 3× Palette) suggest the dispatcher is re-launching workers
  on already-in-progress/already-merged issues. #521 ("`_ready` should exclude issues with active workers")
  is the systemic fix and is worth raising in priority.
- **Goal↔child linkage is incomplete** — #649 (ADR-026) in particular shows no children though #599–#602 exist.
  Without native sub-issue links the Goal progress bars under-report.
- **Status drift** (#590, and the related #586) — doc headers lag code reality across ADR-025/026/027.
  The P0 doc fix addresses ADR-025; the others are worth a sweep.
- **Silent security gap** — Rust M5 (ADR-025 Phase 2) ships in CI with no RBAC interceptor. Harmless while
  Go M5 is production, but any cutover/shadow to Rust M5 would expose an unauthenticated mutation surface.
